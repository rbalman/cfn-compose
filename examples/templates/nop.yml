Parameters:
  EnvironmentType:
    Type: String
  EnvironmentName:
    Type: String
Conditions:
  Never:
    !Equals [ "A", "C" ]

Resources:
  NonResource:
    Type: Custom::NonResource
    Properties:
      ServiceToken: arn:aws:lambda:us-east-1:123456789012:function:mock-function
      One: "key"

  # TestSecurityGroup:
  #   Type: AWS::EC2::SecurityGroup
  #   Properties:
  #     GroupDescription: TestSecurityGroup
  #     VpcId:
  #       Fn::ImportValue: balman:VPC
